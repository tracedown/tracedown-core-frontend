# Defaults for production source builds (`npm run build` without overrides).
# Same-origin paths proxied by the dashboard's nginx. Docker build args override.
VITE_API_URL=/api/v1
VITE_WS_URL=/ws
VITE_WS_MAX_RETRIES=5
